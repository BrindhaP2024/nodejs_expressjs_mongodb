
function add(a, b) {
    return a + b;
  }
  
  function multiply(a, b) {
    return a * b;
  }
  
  function main() {
    const x = 10;
    const y = 20;
  
    const sum = add(x, y);
    console.log(`Sum: ${sum}`);
  
    const product = multiply(x, y);
    console.log(`Product: ${product}`);
  }
  
  main();
  